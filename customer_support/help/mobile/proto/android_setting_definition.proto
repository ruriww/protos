edition = "2023";

package customer_support.help.mobile.proto;

option objc_class_prefix = "CSHM";

message AndroidSettingDefinition {
	enum Type {
		option features.enum_type = CLOSED;

		UNKNOWN = 0;
		SWITCH = 1;
		INTENT = 2;
	}

	Type type = 1;
	string key = 2;
}
