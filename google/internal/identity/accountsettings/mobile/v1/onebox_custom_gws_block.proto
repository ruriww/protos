edition = "2023";

package google.internal.identity.accountsettings.mobile.v1;

option objc_class_prefix = "ASM";

message OneboxCustomGwsBlock {
	enum Type {
		option features.enum_type = CLOSED;

		UNKNOWN = 0;
		SAFESEARCH = 1;
	}

	Type type = 1;
}
